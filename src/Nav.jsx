import React from "react";

const Nav = (props) => {
    return (
        <nav id="nav">
            <a href="/login">Login</a>
            <a href="/register">Signup</a>
            <a href="https://landbnb.netlify.app/">Home</a>
        </nav>
    )
}

export default Nav;